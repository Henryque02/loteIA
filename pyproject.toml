[project]
name = "loteia"
version = "0.1.0"
description = "Simulador probabilístico de viabilidade de loteamentos (preço/m² por ML + Monte Carlo)."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.3",
    "geopandas>=1.1.3",
    "joblib>=1.5.3",
    "mapie>=1.4.0",
    "matplotlib>=3.10.9",
    "numpy>=2.4.6",
    "openpyxl>=3.1.5",
    "pandas>=3.0.3",
    "pyarrow>=24.0.0",
    "pydantic>=2.13.4",
    "pyogrio>=0.12.1",
    "requests>=2.34.2",
    "scikit-learn>=1.9.0",
    "shap>=0.52.0",
    "streamlit>=1.58.0",
    "uvicorn>=0.48.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "ipykernel>=7.2.0",
    "nbconvert>=7.17.1",
    "nbformat>=5.10.4",
    "pytest>=9.0.3",
    "ruff>=0.15.15",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/loteia"]